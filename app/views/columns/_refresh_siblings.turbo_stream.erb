<% column.sibling_columns.each do |sibling_column| %>
  <%= turbo_stream.replace(dom_id(sibling_column), partial: "boards/show/column", method: :morph, locals: { column: sibling_column }) %>
<% end %>

