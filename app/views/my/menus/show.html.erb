<%= turbo_frame_tag "my_menu", target: "_top" do %>
  <div class="flex gap">
    <%= text_field_tag :search, nil,
          type: "search",
          role: "combobox",
          placeholder: "Type to jump to a collection, person, place, or tag…",
          class: "input input--transparent txt-small",
          autofocus: true,
          autocorrect: "off",
          autocomplete: "off",
          aria: { activedescendant: "" },
          data: {
            "1p-ignore": "true",
            filter_target: "input",
            nav_section_expander_target: "input",
            navigable_list_target: "input",
            action: "input->filter#filter" } %>
    <button class="fizzy-menu__close btn borderless txt-small" data-action="dialog#close">
      <%= icon_tag "close" %>
      <span class="for-screen-reader">Close menu</span>
    </button>
  </div>

  <%= render "my/menus/hotkeys" %>

  <div class="margin-block-start-half txt-x-small txt-align-center txt-subtle">
    BOXCAR is designed, built, and backed by <strong class="txt-nowrap"><%= icon_tag "37signals" %> <a href="https://37signals.com" target="_blank" class="txt-subtle">37signals</a>.</strong>
  </div>

  <%= render "my/menus/custom", filters: @user_filtering.filters %>
  <%= render "my/menus/collections", user_filtering: @user_filtering %>
  <%= render "my/menus/tags", user_filtering: @user_filtering %>
  <%= render "my/menus/users", user_filtering: @user_filtering %>
  <%= render "my/menus/places", user_filtering: @user_filtering %>
  <%= render "my/menus/accounts", user_filtering: @user_filtering %>

  <%= render "my/menus/shortcut_notice" %>
<% end %>
